%link{href: "#{File.join(root_url, 'assets', 'application.css')}", rel: "stylesheet", type: "text/css"}
%script{src: "#{File.join(root_url, 'assets','application.js')}", type: "text/javascript"}

.search-index
  = render "flight_form"

  %ul#search-results

= render "flight_template"